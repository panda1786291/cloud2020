server:
  port: 9002
spring:
  application:
    name: nacaos-payment-provider

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #注册进入nacos中心
management:
  endpoints:
    web:
      exposure:
        include: '*'